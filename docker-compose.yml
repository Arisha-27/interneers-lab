version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: product_mongodb
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: productdb
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: